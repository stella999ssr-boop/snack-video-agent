# DashScope API Key（通义千问 + 通义万相）
# 从 https://dashscope.console.aliyun.com/ 获取
DASHSCOPE_API_KEY=sk-your-key-here

# 运行模式: demo（本地规则，无需 API） / live（LLM + 视频生成）
AGENT_MODE=demo

# live 模式下是否生成 Wan2.2 视频（耗时且产生费用）
LIVE_ENABLE_VIDEO=false

# 服务地址和端口
HOST=127.0.0.1
PORT=8000
